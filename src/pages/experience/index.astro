---
import { EXPERIENCE } from "../../data/experience";
import { SITE } from "../../data/site";
import Layout from "../../layouts/Layout.astro";
import Icon from "../../components/icons/Icon.astro";
---

<Layout
	title="Experience — Vlad Setchin"
	description="25+ years of full-stack engineering across medical, educational, field service, utilities, retail, and wholesale — from startups to enterprise scale."
>
	<div
		class="container section"
		style="padding-top: var(--space-16); padding-bottom: var(--space-24);"
	>
		<h1 class="section__title" style="margin-bottom: var(--space-4);">
			<span class="section__title-icon"
				><Icon name="briefcase" size={18} /></span
			>
			<span class="section__title-text">Experience</span>
		</h1>
		<p class="experience-intro">
			25+ years of full-stack engineering across medical, educational, field
			service, utilities, retail, and wholesale — from startups to enterprise
			scale.
		</p>

		<div class="timeline">
			{
				EXPERIENCE.map((role) => (
					<article class="timeline__item">
						<div class="timeline__marker">
							<span class="timeline__dot" />
						</div>
						<div class="timeline__body">
							<h2 class="timeline__role">{role.product}</h2>
							<p class="timeline__summary">{role.summary}</p>
							<ul class="timeline__highlights">
								{role.highlights.map((h) => (
									<li>{h}</li>
								))}
							</ul>
							<ul class="timeline__tags">
								{role.tech.map((t) => (
									<li>[ {t} ]</li>
								))}
							</ul>
						</div>
					</article>
				))
			}
		</div>

		<div class="experience-cta section">
			<h2 style="margin-bottom: var(--space-4);">Want the full picture?</h2>
			<p
				style="color: var(--color-text-muted); margin-bottom: var(--space-6); font-size: var(--text-lg);"
			>
				Get in touch and I'll send over my CV.
			</p>
			<a href={`mailto:${SITE.email}`} class="cta-btn">Get in touch</a>
			<a
				href={SITE.linkedin}
				target="_blank"
				rel="noopener noreferrer"
				class="cta-btn cta-btn--secondary"
			>
				Connect on LinkedIn
			</a>
		</div>
	</div>
</Layout>

<style>
	.experience-intro {
		font-size: var(--text-xl);
		color: var(--color-text-muted);
		max-width: 640px;
		margin-bottom: var(--space-12);
		line-height: 1.7;
	}

	.timeline {
		position: relative;
		display: flex;
		flex-direction: column;
		gap: var(--space-12);
		margin-bottom: var(--space-16);
	}

	.timeline::before {
		content: "";
		position: absolute;
		top: 6px;
		bottom: 6px;
		left: 10px;
		width: 1px;
		background: var(--color-border);
	}

	.timeline__item {
		display: grid;
		grid-template-columns: 20px 1fr;
		column-gap: var(--space-6);
		padding-bottom: var(--space-12);
		border-bottom: 1px solid var(--color-border);
	}

	.timeline__item:last-child {
		border-bottom: none;
		padding-bottom: 0;
	}

	.timeline__marker {
		position: relative;
		display: flex;
		justify-content: center;
	}

	.timeline__dot {
		width: 9px;
		height: 9px;
		border-radius: 50%;
		background: var(--color-accent);
		box-shadow:
			0 0 0 3px var(--color-bg),
			0 0 8px rgba(0, 224, 138, 0.45);
		margin-top: 0.3em;
		position: relative;
		z-index: 1;
	}

	.timeline__role {
		font-size: var(--text-xl);
		color: var(--color-heading);
		margin-bottom: var(--space-3);
	}

	.timeline__summary {
		color: var(--color-text-muted);
		font-size: var(--text-base);
		line-height: 1.7;
		margin-bottom: var(--space-4);
		max-width: 700px;
	}

	.timeline__highlights {
		list-style: none;
		padding: 0;
		margin: 0 0 var(--space-4) 0;
		max-width: 700px;
	}

	.timeline__highlights li {
		color: var(--color-text-muted);
		font-size: var(--text-sm);
		padding: var(--space-1) 0;
		padding-left: var(--space-4);
		position: relative;
		line-height: 1.6;
	}

	.timeline__highlights li::before {
		content: "→";
		position: absolute;
		left: 0;
		color: var(--color-accent);
		font-size: var(--text-xs);
		top: 0.5em;
	}

	.timeline__tags {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-2);
		padding: 0;
		margin: 0;
	}

	.timeline__tags li {
		font-size: var(--text-xs);
		color: var(--color-text-muted);
		padding: var(--space-1) var(--space-2);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-sm);
		background: var(--color-surface);
		white-space: nowrap;
	}

	.experience-cta {
		border-top: 1px solid var(--color-border);
		padding-top: var(--space-12);
		text-align: center;
	}

	.cta-btn {
		display: inline-flex;
		align-items: center;
		padding: var(--space-3) var(--space-8);
		border-radius: var(--radius-md, 0.5rem);
		font-weight: 600;
		font-size: var(--text-base);
		text-decoration: none;
		transition: opacity 0.2s ease;
		margin: var(--space-2);
	}

	.cta-btn:hover {
		opacity: 0.85;
	}

	.cta-btn:not(.cta-btn--secondary) {
		background: var(--color-accent);
		color: #04130c;
	}

	.cta-btn--secondary {
		border: 1px solid var(--color-border);
		color: var(--color-text);
	}

	@media (max-width: 640px) {
		.timeline__item {
			grid-template-columns: 1fr;
			gap: var(--space-3);
		}

		.timeline__marker,
		.timeline::before {
			display: none;
		}
	}
</style>
